# React Amplify authentication wrapper

React authentication wrapper based on top of AWS Amplify

## Install

with NPM:

```bash
npm install @ivyhjk/amplify-react-auth
```

with yarn:

```bash
yarn add @ivyhjk/amplify-react-auth
```
