# React Amplify federated authentication

React federated authentication based on top of AWS Amplify

## Install

with NPM:

```bash
npm install @ivyhjk/amplify-react-federated-auth
```

with yarn:

```bash
yarn add @ivyhjk/amplify-react-federated-auth
```
